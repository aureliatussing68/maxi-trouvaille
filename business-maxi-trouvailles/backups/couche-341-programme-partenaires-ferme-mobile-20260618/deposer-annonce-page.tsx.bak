import type { Metadata } from "next";
import Link from "next/link";
import { ClipboardList } from "lucide-react";
import { PageHeader } from "@/components/PageHeader";

export const metadata: Metadata = {
  title: "Programme partenaires",
  robots: {
    index: false,
    follow: false,
  },
};

export default function CreateListingPage() {
  return (
    <>
      <PageHeader
        eyebrow="Programme partenaires"
        title="Programme partenaires Maxi Trouvaille"
        description="Le parcours partenaire professionnel sera ajouté après validation. La boutique produits partenaires reste prioritaire."
      />
      <section className="container-page py-10">
        <div className="rounded-lg border border-line bg-paper p-6 shadow-sm">
          <ClipboardList className="mb-4 text-teal" size={30} aria-hidden="true" />
          <h2 className="text-2xl font-black">Espace partenaire en préparation</h2>
          <p className="mt-3 max-w-2xl text-sm leading-6 text-muted">
            Cette page reserve l&apos;emplacement du futur parcours partenaire. A terme,
            elle utilisera un compte professionnel, un statut de validation, des
            photos, un prix, une categorie et une verification admin avant publication.
          </p>
          <div className="mt-6 grid gap-4 md:grid-cols-2">
            <div className="rounded-lg border border-dashed border-line p-4">
              <h3 className="font-black">Champs prevus</h3>
              <p className="mt-2 text-sm leading-6 text-muted">
                Titre, description, prix, etat, categorie, photos, stock,
                livraison et informations partenaire.
              </p>
            </div>
            <div className="rounded-lg border border-dashed border-line p-4">
              <h3 className="font-black">Workflow prevu</h3>
              <p className="mt-2 text-sm leading-6 text-muted">
                Sélection, attente de validation, approbation ou refus par un
                administrateur.
              </p>
            </div>
          </div>
          <Link
            href="/produits-partenaires"
            className="focus-ring mt-6 inline-flex min-h-11 items-center rounded-md bg-foreground px-5 py-2.5 text-sm font-black text-white hover:bg-[#2b2b2b]"
          >
            Voir les produits partenaires
          </Link>
        </div>
      </section>
    </>
  );
}
